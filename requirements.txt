requests>=2.32,<3
pandas>=2.2,<3
Flask>=3.1,<4
Flask-WTF>=1.2,<2
Werkzeug>=3.1,<4
gunicorn>=21,<23
gunicorn
waitress>=3,<4  # Windows masaüstü başlatıcısı (gunicorn Windows'ta yok)
truststore>=0.9; sys_platform == "win32"  # Windows sertifika deposu — antivirüs/proxy SSL denetimi (verify hep AÇIK)
