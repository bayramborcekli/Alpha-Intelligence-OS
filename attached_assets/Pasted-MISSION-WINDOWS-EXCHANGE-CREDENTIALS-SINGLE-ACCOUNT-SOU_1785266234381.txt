MISSION — WINDOWS EXCHANGE CREDENTIALS + SINGLE ACCOUNT SOURCE OF TRUTH

AMAÇ

Alpha Intelligence OS'ta Binance Global Spot ve Binance TR Spot hesap
bağlantı mimarisini tek ve kalıcı yapıya indir.

Bu görev iki problemi BİRLİKTE çözecek:

A) Windows temiz kurulum / credential lifecycle problemi
B) Aynı Binance hesap/cüzdan verisinin farklı ekranlarda farklı backend
   yollarından üretilmesi problemi

SON HEDEF:

Ayarlar → Hesaplarım
hesap bağlantılarının TEK yönetim noktası olacak.

Diğer ekranlar burada üretilen canonical account snapshot'ı yalnız okuyacak.

==================================================
1. MEVCUT GERÇEK WINDOWS BULGUSU
==================================================

Güncel GitHub main temiz Windows clone'u çalışıyor.

Windows Local Auth düzeltmesi başarılı:
- İlk Kurulum döngüsü çözüldü
- local admin sistemi çalışıyor

Ancak temiz clone'da .env yoktu.

Eski çalışan Windows .env yeni clone'a kopyalandıktan sonra güvenli metadata
testi:

BINANCE_GLOBAL_API_Key = YOK
BINANCE_GLOBAL_Secret_Key = YOK
BINANCE_TR_API_KEY = OK
BINANCE_TR_API_SECRET = OK

Sonuç:

Binance TR credential'ları mevcut ve okunuyor.

Binance Global tarafında legacy/canonical ENV isim uyuşmazlığı var.

Replit'te ise Binance Global Spot ve Binance TR Spot daha önce canlı
HEALTHY doğrulandı.

==================================================
2. DAHA ÖNCE TESPİT EDİLEN MİMARİ PROBLEM
==================================================

Daha önce aynı Binance hesaplarının:

- Ayarlar → Hesaplarım
- Trading Home / Overview
- Portfolio
- header/status alanları

arasında farklı sonuçlar ürettiği görüldü.

Örnek gerçek davranış:

Bir ekranda:
Binance Global = HEALTHY

başka ekranda:
Binance Global = FAILED / UNKNOWN

Bu kabul edilemez.

Sebep olabilecek duplicate yolları repository genelinde tara:

- credential resolver
- account fetcher
- wallet fetcher
- balance calculator
- connection test
- health check
- sync logic
- account snapshot
- dashboard-specific Binance calls

Önce inventory çıkar.

==================================================
3. TEK YÖNETİM NOKTASI
==================================================

Ayarlar → Hesaplarım hesap bağlantılarının TEK yönetim noktasıdır.

Buradan yönetilecek:

- credential configuration
- connection test
- account synchronization
- wallet retrieval
- balance retrieval
- Spot permission
- account health
- last sync
- primary account selection

Binance Global Spot ve Binance TR Spot için aynı prensip geçerli.

==================================================
4. TEK BACKEND SOURCE OF TRUTH
==================================================

UI sayfasını veri kaynağı yapma.

Backend'de TEK canonical Account/Exchange Service oluştur veya mevcut
uygun servisi bu role getir.

Mimari:

Binance Global Spot API
          |
          |
          v
Canonical Account/Exchange Service
          ^
          |
          |
Binance TR Spot API

Canonical service
          |
          v
Canonical Account Snapshot
          |
    +-----+------+---------+---------+
    |            |         |         |
 Settings     Trading   Portfolio   Header
 Accounts      Home                 Status

Ayarlar → Hesaplarım bu servisin yönetim UI'sıdır.

Trading Home / Portfolio / Overview / Header bu servisin snapshot'ını
SADECE OKUR.

==================================================
5. DUPLICATE EXCHANGE CALL YASAĞI
==================================================

Trading Home, Overview, Portfolio, Header veya başka UI endpoint'i:

Binance'a kendi başına ikinci bağlantı KURMAYACAK.

Aynı hesap için:

- ikinci credential resolver
- ikinci wallet fetcher
- ikinci balance fetcher
- ikinci health checker
- ikinci synchronization mechanism

OLMAYACAK.

Repository genelindeki duplicate yolları sınıflandır:

REMOVE
DELEGATE
TOMBSTONE

Hangisine karar verildiğini final raporda yaz.

==================================================
6. CANONICAL CREDENTIAL RESOLVER
==================================================

Tek credential resolver kullanılacak.

Binance Global canonical isimleri:

BINANCE_GLOBAL_API_Key
BINANCE_GLOBAL_Secret_Key

Binance TR canonical isimleri:

BINANCE_TR_API_KEY
BINANCE_TR_API_SECRET

Aktif exchange client'lar farklı env isimlerini kendi başlarına aramasın.

Credential çözümleme merkezi resolver üzerinden yapılsın.

==================================================
7. LEGACY ENV MIGRATION
==================================================

Repository geçmişinde kullanılan Binance Global alias isimlerini tara.

Örnek:

BINANCE_API_KEY
BINANCE_API_SECRET
BINANCE_API_Key
BINANCE_Secret_Key

ve gerçekten kullanılmış diğer legacy isimler.

Legacy alias desteği yalnız:

migration
veya
backward compatibility

katmanında bulunabilir.

Canonical isim mevcutsa canonical HER ZAMAN kazanır.

Legacy alias runtime boyunca ikinci source-of-truth haline gelmesin.

==================================================
8. WINDOWS CREDENTIAL STORAGE
==================================================

Temiz Git clone'da .env olmaması normaldir.

Secrets GitHub'a KONMAMALI.

Kullanıcıdan artık:

- eski clone'dan manuel .env kopyalaması
- Notepad ile .env düzenlemesi
- PowerShell ile env adı değiştirmesi

beklenmemeli.

Windows'ta:

Ayarlar
→ Hesaplarım
→ Binance Global / Binance TR
→ Düzenle

üzerinden credential güvenli biçimde kaydedilebilmeli.

Mevcut güvenli local credential storage yaklaşımı varsa onu kullan.

Yeni storage oluşturulacaksa:

- project-local
- gitignored
- atomic write
- plaintext UI/log sızıntısı yok
- güvenli file permission
- symlink/path traversal koruması

sağlanmalı.

==================================================
9. WINDOWS LOCAL ADMIN AUTH İLE AYRI KALACAK
==================================================

ÇOK ÖNEMLİ:

data/local_admin.json

yalnız Windows admin login içindir.

Exchange credential'ları bunun içine KONMAYACAK.

İki ayrı güvenlik alanı:

WINDOWS AUTH
ve
EXCHANGE CREDENTIALS

birbirine karıştırılmayacak.

==================================================
10. REPLIT DAVRANIŞI
==================================================

Replit'te:

Replit Secrets / process env canonical source olarak kalacak.

Çalışan:

Binance Global Spot
Binance TR Spot

bağlantılarını bozma.

Windows credential çözümlemesi Replit Secrets davranışını değiştirmemeli.

==================================================
11. ACCOUNT STATE MODEL
==================================================

UNKNOWN her durumda kullanılmasın.

Canonical account state açık şekilde ayırsın:

NOT_CONFIGURED
CONNECTING
HEALTHY
AUTH_FAILED
CONNECTION_FAILED
STALE
DISABLED

Örneğin credential hiç yoksa:

NOT_CONFIGURED

olmalı.

Credential yokken:

TLS ERROR
AUTH FAILED
UNKNOWN

gibi yanıltıcı teşhis üretme.

==================================================
12. CANONICAL ACCOUNT SNAPSHOT
==================================================

Her hesap için tek snapshot modeli oluştur/kullan.

Örnek alanlar:

exchange
account_type
configured
connection_state
health
spot_enabled
trading_permission
wallet_count
portfolio_value_usdt
available_usdt
locked_usdt
asset_count
last_sync
data_freshness
error_code
error_message

Secret/API key/signature snapshot içinde bulunmayacak.

==================================================
13. AYARLAR → HESAPLARIM
==================================================

Bu ekran hesap yönetiminin TEK yeri.

Buradan:

- Düzenle
- Bağlantı Testi
- Eşitle
- Cüzdanları Yenile
- Birincil Yap

işlemleri yapılabilir.

Buradaki sonuç canonical service/snapshot'a yazılmalı.

Başka ekran kendi Binance sonucunu üretmemeli.

==================================================
14. DİĞER EKRANLAR
==================================================

Trading Home:

yalnız canonical snapshot okur.

Portfolio:

aynı canonical snapshot/account service üzerinden detay alır.

Overview:

aynı canonical snapshot okur.

Header:

aynı canonical health state okur.

Dolayısıyla:

Settings Global = HEALTHY

iken:

Trading Home Global = FAILED

OLAMAZ.

==================================================
15. DATA CONSISTENCY CONTRACT
==================================================

Aynı sync generation için:

Settings health
=
Trading Home health
=
Portfolio health
=
Header health

Wallet count aynı source'tan.

Portfolio value aynı source'tan.

Last sync aynı source'tan.

Spot permission aynı source'tan.

Farklı ekranların farklı zamanlarda render olması nedeniyle freshness farkı
olabilir; fakat aynı generation/snapshot için çelişkili state olamaz.

==================================================
16. CACHE / REFRESH
==================================================

Refresh davranışını merkezi yap.

"Tümünü Yenile"
veya
"Hesabı Eşitle"

canonical service'i refresh eder.

Sonra UI'lar yeni snapshot'ı okur.

Her widget kendi Binance API çağrısını başlatmasın.

Concurrent refresh duplicate exchange request storm üretmesin.

Gerekirse single-flight/locking kullan.

==================================================
17. SECURITY
==================================================

ASLA gösterme/loglama:

API secret
full API key
signature
password
authorization header

UI gerekiyorsa API key'i yalnız maskeli gösterebilir.

Örnek:

5415************

Error mesajları sanitize edilmeli.

==================================================
18. SPOT-ONLY KORUNACAK
==================================================

Görev #45 Spot-only kararı korunacak.

Futures EKLEME.

Margin EKLEME.

Withdraw EKLEME.

Transfer EKLEME.

Binance Global Spot korunacak.
Binance TR Spot korunacak.
PAPER korunacak.

Eski Futures kodunu geri getirme.

==================================================
19. LIVE TRADING YOK
==================================================

Bu görevde gerçek order endpoint EKLEME.

Risk profile EKLEME.

LIVE_MICRO EKLEME.

Bunlar sonraki görevlerdir.

Bu görev yalnız:

credential lifecycle
+
account architecture
+
single source of truth

içindir.

==================================================
20. WINDOWS CLEAN INSTALL ACCEPTANCE
==================================================

Gerçek hedef akış:

Fresh Git clone
→ INSTALL_WINDOWS.cmd
→ Windows local admin setup
→ login
→ /home
→ Ayarlar/Hesaplarım
→ Binance Global credential gir
→ Binance TR credential gir
→ kaydet
→ Bağlantı Testi
→ Global HEALTHY
→ TR HEALTHY
→ Cüzdanları Yenile
→ snapshot oluşur
→ Trading Home aynı HEALTHY state'i gösterir
→ Portfolio aynı account verisini gösterir
→ restart
→ login
→ credential'lar korunur
→ Global HEALTHY
→ TR HEALTHY

Bu akışta manuel .env işlemi OLMAMALI.

==================================================
21. LEGACY WINDOWS ACCEPTANCE
==================================================

Eski çalışan .env içeriği varsa:

legacy Global isimleri algılanmalı.

Migration sonrası canonical resolver:

GLOBAL KEY PRESENT
GLOBAL SECRET PRESENT
TR KEY PRESENT
TR SECRET PRESENT

olarak doğrulamalı.

Gerçek değerler raporlanmamalı.

==================================================
22. TESTLER
==================================================

Mutlaka test et:

1. fresh Windows clone + .env yok
   -> app çalışır

2. credential yok
   -> NOT_CONFIGURED

3. Global legacy env isimleri
   -> resolver bulur

4. Global canonical isimleri
   -> resolver bulur

5. TR canonical isimleri
   -> resolver bulur

6. canonical > legacy priority

7. Windows Settings üzerinden credential save
   -> restart sonrası korunur

8. Git pull/update
   -> credential korunur

9. Replit Secrets davranışı değişmez

10. local_admin.json exchange secret içermez

11. Settings Global HEALTHY
    -> Trading Home Global aynı snapshot HEALTHY

12. Settings TR HEALTHY
    -> Trading Home TR aynı snapshot HEALTHY

13. Settings wallet_count
    =
    diğer ekranlardaki wallet_count

14. portfolio value aynı snapshot'tan

15. last_sync aynı snapshot'tan

16. duplicate wallet fetch yok

17. duplicate health check yok

18. duplicate credential resolver yok

19. concurrent refresh duplicate request üretmez

20. secret/signature loglarda yok

21. Futures endpoint yok

22. PAPER regresyonu yok

23. Binance Global Spot read-only path çalışır

24. Binance TR Spot read-only path çalışır

25. full regression = 0 FAIL

==================================================
23. ARCHITECTURE GUARD TEST
==================================================

Yeni bir mimari bekçi testi ekle.

Bu test repository'de:

- UI/dashboard katmanından doğrudan Binance credential çözümleme
- duplicate signed account fetch
- duplicate wallet fetch
- eski Global env isimlerinin aktif runtime client'larda doğrudan kullanımı
- Futures authenticated endpoint

yeniden eklenirse FAIL versin.

Ama public PAPER market-data kullanımlarını yanlış pozitif olarak engelleme.

==================================================
24. ÖNCE İNCELE, SONRA DEĞİŞTİR
==================================================

Koda başlamadan önce repository inventory çıkar.

Raporla:

CURRENT credential paths
CURRENT wallet paths
CURRENT health paths
CURRENT snapshot paths
DUPLICATES
LEGACY paths

Sonra minimum refactor yap.

Çalışan Binance TR ve Global client'larını gereksiz yeniden yazma.

==================================================
25. TESLİM RAPORU
==================================================

Final raporda tam olarak:

1. ROOT CAUSE
2. DUPLICATE PATH INVENTORY
3. LEGACY ENV NAMES FOUND
4. CANONICAL CREDENTIAL NAMES
5. SINGLE ACCOUNT SERVICE ARCHITECTURE
6. WINDOWS STORAGE
7. LEGACY MIGRATION
8. CANONICAL SNAPSHOT MODEL
9. REMOVED/DELEGATED/TOMBSTONED PATHS
10. SETTINGS RESULT
11. TRADING HOME RESULT
12. PORTFOLIO RESULT
13. REPLIT RESULT
14. WINDOWS RESULT
15. SECURITY REVIEW
16. ARCHITECTURE GUARD RESULT
17. TARGETED TESTS
18. FULL REGRESSION
19. CHANGED FILES
20. REMAINING RISKS
21. GIT COMMIT HASH

Tam regresyon 0 FAIL olmadan tamamlandı sayma.

GitHub main'e push et.